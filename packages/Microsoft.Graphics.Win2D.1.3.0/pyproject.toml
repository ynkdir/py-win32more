[project]
name = "win32more-Microsoft.Graphics.Win2D"
version = "0.7.1.3.0"
dependencies = ["win32more-core==0.7.*", "win32more-Microsoft.WindowsAppSDK>=0.7.1.6.240923002,<0.8"]

[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/win32more"]
