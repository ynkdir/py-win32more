[project]
name = "win32more-Microsoft.Graphics.Win2D"
version = "0.8.1.3.1"
dependencies = ["win32more-core==0.8.*", "win32more-Microsoft.WindowsAppSDK>=0.8.1.6.241114003,<0.9"]

[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/win32more"]
