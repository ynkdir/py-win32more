[project]
name = "win32more-Microsoft.Windows.WDK.Win32Metadata"
version = "0.8.0.13.25"
dependencies = ["win32more-core==0.8.*", "win32more-Microsoft.Windows.SDK.Win32Metadata>=0.8.63.0.31-preview,<0.9"]

[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/win32more"]
