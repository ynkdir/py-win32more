[project]
name = "win32more-Microsoft.Graphics.Win2D"
version = "0.6.1.3.2"
dependencies = ["win32more==0.6.*", "win32more-Microsoft.WindowsAppSDK>=0.6.1.6.241114003,<0.7"]

[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/win32more"]
